# ─── DEV environment ───────────────────────────────
# Vite loads this automatically when running: npm run dev
#
# VITE_API_URL MUST be empty in dev!
# → All /api/* fetches stay on localhost:5173
# → Vite proxy forwards them to localhost:5555 (backend)
# → Cookies work (same-origin) → admin login works
#
# If this is set to a prod URL, EVERYTHING breaks in dev:
#   rooms, gallery, admin auth, booking — all of it.

VITE_API_URL=http://localhost:5555
VITE_WITH_CREDENTIALS=false
