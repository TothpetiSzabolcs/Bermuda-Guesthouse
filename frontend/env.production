# ─── PRODUCTION environment ────────────────────────
# Vite loads this when running: npm run build
#
# Set VITE_API_URL to your actual backend URL (no trailing slash!)
# This is needed because in production the frontend (Vercel/Netlify)
# and backend run on separate domains.

VITE_API_URL=https://bermuda-vendeghaz.hu
VITE_WITH_CREDENTIALS=true
